---
import Base from '../layouts/Base.astro';
const base = import.meta.env.BASE_URL;

const brands = [
  'Glossier', 'True Classic', 'Everlane', 'Vuori', 'Tommy John',
  'Dr. Squatch', 'Caraway', 'BaubleBar', '32 Degrees', 'Wyze',
  'Jaxxon', 'Olly', 'Simple Modern', 'Suit Shop', 'Taylor Stitch',
  'Fanatics Live', 'Nathan James', 'Carpe'
];

const episodes = [
  { title: 'How 8 CX Leaders Are Making AI Work — Right Now', guests: 'Glossier, Dr. Squatch, Caraway & more', tag: 'Bonus', num: '01' },
  { title: '0 to 100: Building CX From Day One', guests: 'Breanna Moreno & Dan Kaparski', tag: 'S2', num: '02' },
  { title: 'Quiet Luxury, Human CX', guests: 'Everlane & Vuori', tag: 'S2', num: '03' },
];

const features = [
  { icon: '&#x26A1;', title: 'Curated Events', desc: 'Intimate gatherings at the industry\'s biggest moments' },
  { icon: '&#x1F399;', title: 'Real Conversations', desc: 'Operators sharing what actually works — no script' },
  { icon: '&#x1F310;', title: 'Trusted Community', desc: 'A network that skips the pitch and shares the playbook' },
  { icon: '&#x2728;', title: 'Zero Sales Energy', desc: 'Connection without the noise. Just great people, period.' },
];
---

<Base title="CX Mixer | E-commerce Events, Community & Podcast">

  <!-- ═══ HERO ═══ -->
  <section class="hero">
    <div class="hero__bg" aria-hidden="true">
      <div class="hero__orb hero__orb--1"></div>
      <div class="hero__orb hero__orb--2"></div>
      <div class="hero__orb hero__orb--3"></div>
      <div class="hero__grid"></div>
      <!-- Decorative floating elements -->
      <div class="hero__deco hero__deco--star">&#x2726;</div>
      <div class="hero__deco hero__deco--cross">+</div>
      <div class="hero__deco hero__deco--ring"></div>
    </div>

    <div class="container hero__content">
      <div class="hero__eyebrow" style="animation: slide-up 0.6s var(--ease) both;">
        <span class="badge badge--outline">Events &middot; Community &middot; Podcast</span>
      </div>

      <h1 class="hero__title" style="animation: slide-up 0.7s var(--ease) 0.1s both;">
        Where e-commerce<br/>brands connect
        <span class="hero__em"><em class="serif">beyond the pitch</em></span>
      </h1>

      <p class="hero__sub" style="animation: slide-up 0.7s var(--ease) 0.2s both;">
        CX Mixer brings together brand leaders, operators, and industry friends through curated events, honest conversations, and content that makes the e-commerce world feel a little smaller and a lot more human.
      </p>

      <div class="hero__actions" style="animation: slide-up 0.7s var(--ease) 0.3s both;">
        <a href={`${base}collective/`} class="btn btn--primary">Join the Collective</a>
        <a href={`${base}events/`} class="btn btn--ghost">See Upcoming Events</a>
      </div>

      <div class="hero__listen" style="animation: fade-in 1s var(--ease) 0.6s both;">
        <span class="hero__listen-label">Listen on</span>
        <a href="https://open.spotify.com/show/4a2lEUxl4dGm4CqdNm3HvZ" target="_blank" rel="noopener" class="hero__listen-icon" aria-label="Spotify">
          <svg width="26" height="26" viewBox="0 0 24 24" fill="currentColor"><path d="M12 0C5.4 0 0 5.4 0 12s5.4 12 12 12 12-5.4 12-12S18.6 0 12 0zm5.5 17.3c-.2.3-.6.4-1 .2-2.7-1.7-6.1-2-10.1-1.1-.4.1-.8-.1-.9-.5-.1-.4.1-.8.5-.9 4.4-1 8.1-.6 11.2 1.3.3.2.4.7.3 1zm1.5-3.3c-.3.4-.8.5-1.2.3-3.1-1.9-7.8-2.5-11.5-1.3-.5.1-1-.1-1.1-.6-.1-.5.1-1 .6-1.1 4.2-1.3 9.4-.7 13 1.5.3.2.5.7.2 1.2zm.1-3.4c-3.7-2.2-9.9-2.4-13.4-1.3-.5.2-1.1-.1-1.3-.7-.2-.5.1-1.1.7-1.3 4.1-1.2 10.8-1 15 1.5.5.3.7.9.4 1.4-.3.5-.9.6-1.4.4z"/></svg>
        </a>
        <a href="https://www.youtube.com/@CXMixer" target="_blank" rel="noopener" class="hero__listen-icon" aria-label="YouTube">
          <svg width="28" height="28" viewBox="0 0 24 24" fill="currentColor"><path d="M23.5 6.2a3 3 0 0 0-2.1-2.1C19.5 3.5 12 3.5 12 3.5s-7.5 0-9.4.6A3 3 0 0 0 .5 6.2 31.9 31.9 0 0 0 0 12a31.9 31.9 0 0 0 .5 5.8 3 3 0 0 0 2.1 2.1c1.9.6 9.4.6 9.4.6s7.5 0 9.4-.6a3 3 0 0 0 2.1-2.1c.4-1.9.5-5.8.5-5.8s0-3.9-.5-5.8zM9.5 15.6V8.4l6.3 3.6-6.3 3.6z"/></svg>
        </a>
        <a href="https://podcasts.apple.com/us/podcast/the-cx-mixer" target="_blank" rel="noopener" class="hero__listen-icon" aria-label="Apple Podcasts">
          <svg width="24" height="24" viewBox="0 0 24 24" fill="currentColor"><path d="M12 0C5.4 0 0 5.4 0 12s5.4 12 12 12 12-5.4 12-12S18.6 0 12 0zm0 3.6c4.6 0 8.4 3.8 8.4 8.4 0 2.4-1 4.5-2.6 6.1-.3-.9-.7-1.6-1.2-2.3 1-1.1 1.6-2.4 1.6-3.8 0-3.4-2.8-6.2-6.2-6.2S5.8 8.6 5.8 12c0 1.4.6 2.8 1.6 3.8-.5.6-.9 1.4-1.2 2.3C4.6 16.5 3.6 14.4 3.6 12c0-4.6 3.8-8.4 8.4-8.4zm0 4.8c-2 0-3.6 1.6-3.6 3.6 0 1.2.6 2.3 1.5 2.9-.2.5-.3 1-.4 1.5-.1.6 0 1.2.1 1.7l.8 3.6c.2.7.8 1.3 1.6 1.3s1.4-.5 1.6-1.3l.8-3.6c.2-.5.2-1.1.1-1.7-.1-.5-.2-1-.4-1.5.9-.7 1.5-1.7 1.5-2.9 0-2-1.6-3.6-3.6-3.6zm0 2.4a1.2 1.2 0 1 1 0 2.4 1.2 1.2 0 0 1 0-2.4z"/></svg>
        </a>
      </div>
    </div>

    <!-- Scroll indicator -->
    <div class="hero__scroll" aria-hidden="true" style="animation: fade-in 1.2s var(--ease) 1s both;">
      <span>Scroll</span>
      <div class="hero__scroll-line"></div>
    </div>
  </section>

  <!-- ═══ INTRO ═══ -->
  <section class="section intro" id="intro">
    <div class="container">
      <div class="intro__layout">
        <div class="intro__text reveal">
          <span class="section-label">What is CX Mixer</span>
          <h2 class="text-balance">Built for the people <em class="serif">behind</em> the brands</h2>
          <p class="mb-md">
            CX Mixer is where e-commerce leaders come to meet good people, share what's actually working, and build relationships that go beyond a badge scan. From curated conference-side events to podcast conversations and a growing community of operators — this is a space designed for connection without the noise.
          </p>
          <a href={`${base}collective/`} class="btn btn--teal">Explore the Community</a>
        </div>
        <div class="intro__cards">
          {features.map((f, i) => (
            <div class={`intro__card reveal reveal-delay-${i + 1}`}>
              <span class="intro__card-icon" set:html={f.icon} />
              <div>
                <h4>{f.title}</h4>
                <p>{f.desc}</p>
              </div>
            </div>
          ))}
        </div>
      </div>
    </div>
  </section>

  <!-- ═══ EVENTS ═══ -->
  <section class="section section--dark events" id="events">
    <!-- Decorative top border -->
    <div class="divider" style="position:absolute;top:0;left:0;right:0;height:4px;" aria-hidden="true"></div>
    <div class="container">
      <div class="events__head reveal">
        <span class="section-label" style="color: var(--teal);">Events</span>
        <h2>Meet up in <em class="serif">real life</em></h2>
        <p style="color: rgba(255,255,255,0.55); max-width: 50ch;">
          From breakfast meetups to VIP dinners — CX Mixer events are designed to help e-commerce brands connect in a way that actually feels fun. No pitches. Just brands.
        </p>
      </div>
      <div class="events__grid">
        <div class="events__card reveal reveal-delay-1">
          <div class="events__card-num" aria-hidden="true">01</div>
          <span class="badge badge--coral">Breakfast</span>
          <h3>Morning Mixer</h3>
          <p>Start the conference day right. Coffee, conversation, and the kind of intros that actually lead somewhere.</p>
          <span class="events__card-tag">Coming Soon</span>
        </div>
        <div class="events__card events__card--featured reveal reveal-delay-2">
          <div class="events__card-num" aria-hidden="true">02</div>
          <span class="badge badge--coral">Happy Hour</span>
          <h3>The Hangover Hangout</h3>
          <p>A casual mid-conference reset with great food, good people, and zero pressure. Meet fellow brand folks and recharge.</p>
          <span class="events__card-tag">Coming Soon</span>
        </div>
        <div class="events__card reveal reveal-delay-3">
          <div class="events__card-num" aria-hidden="true">03</div>
          <span class="badge badge--coral">VIP Dinner</span>
          <h3>Late Night Conversations</h3>
          <p>An intimate dinner for brand leaders. Curated guest list. No agenda beyond great food and real talk.</p>
          <span class="events__card-tag">Coming Soon</span>
        </div>
      </div>
      <div class="text-center reveal" style="margin-top: var(--sp-xl);">
        <a href={`${base}events/`} class="btn btn--ghost">View All Events</a>
      </div>
    </div>
  </section>

  <!-- ═══ BRAND MARQUEE ═══ -->
  <section class="section brands">
    <div class="container text-center mb-lg reveal">
      <h2>Good people. Great brands.<br/><em class="serif">Real conversations.</em></h2>
    </div>
    <div class="marquee-wrapper">
      <div class="marquee-track">
        {[...brands, ...brands].map((brand) => (
          <span class="brands__pill">{brand}</span>
        ))}
      </div>
    </div>
    <div class="marquee-wrapper" style="margin-top: 0.75rem;">
      <div class="marquee-track" style="animation-direction: reverse; animation-duration: 35s;">
        {[...brands, ...brands].reverse().map((brand) => (
          <span class="brands__pill brands__pill--alt">{brand}</span>
        ))}
      </div>
    </div>
  </section>

  <!-- ═══ COMMUNITY ═══ -->
  <section class="section community">
    <div class="container">
      <div class="community__layout">
        <div class="community__visual reveal">
          <div class="community__blob" aria-hidden="true"></div>
          <div class="community__float community__float--1">
            <span>&#x1F91D;</span> New connection made
          </div>
          <div class="community__float community__float--2">
            <span>&#x1F399;</span> New episode dropped
          </div>
          <div class="community__float community__float--3">
            <span>&#x1F389;</span> Event invite sent
          </div>
        </div>
        <div class="community__text reveal reveal-delay-1">
          <span class="section-label">The CX Collective</span>
          <h2 class="text-balance">A community that sticks <em class="serif">after</em> the event ends</h2>
          <p class="mb-md">
            The CX Collective is the community side of CX Mixer. It's where brand leaders keep the conversations going, meet peers facing similar challenges, and tap into a trusted network of e-commerce operators who get it.
          </p>
          <p class="community__quote mb-lg">
            Think of it as your industry group chat — but with better people and fewer random pitches.
          </p>
          <a href={`${base}collective/`} class="btn btn--teal">Join the Collective</a>
        </div>
      </div>
    </div>
  </section>

  <!-- ═══ PODCAST ═══ -->
  <section class="section section--dark podcast">
    <div class="divider" style="position:absolute;top:0;left:0;right:0;height:4px;" aria-hidden="true"></div>
    <div class="container">
      <div class="podcast__head reveal">
        <span class="section-label" style="color: var(--teal);">Podcast</span>
        <h2>Listen <em class="serif">in</em></h2>
        <p style="color: rgba(255,255,255,0.55); max-width: 50ch;">
          Smart, practical conversations with the people building the customer experience from the inside. Never too polished. Always worth your time.
        </p>
      </div>
      <div class="podcast__list">
        {episodes.map((ep, i) => (
          <a href="https://open.spotify.com/show/4a2lEUxl4dGm4CqdNm3HvZ" target="_blank" rel="noopener" class={`podcast__ep reveal reveal-delay-${i + 1}`}>
            <span class="podcast__ep-num">{ep.num}</span>
            <div class="podcast__ep-info">
              <span class="badge badge--dark" style="margin-bottom:0.4rem;">{ep.tag}</span>
              <h3>{ep.title}</h3>
              <p>{ep.guests}</p>
            </div>
            <div class="podcast__ep-play">
              <svg width="22" height="22" viewBox="0 0 24 24" fill="currentColor"><polygon points="5 3 19 12 5 21 5 3"/></svg>
            </div>
          </a>
        ))}
      </div>
      <div class="text-center reveal" style="margin-top: var(--sp-xl);">
        <div class="podcast__ctas">
          <a href="https://open.spotify.com/show/4a2lEUxl4dGm4CqdNm3HvZ" target="_blank" rel="noopener" class="btn btn--white">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="#1DB954"><path d="M12 0C5.4 0 0 5.4 0 12s5.4 12 12 12 12-5.4 12-12S18.6 0 12 0zm5.5 17.3c-.2.3-.6.4-1 .2-2.7-1.7-6.1-2-10.1-1.1-.4.1-.8-.1-.9-.5-.1-.4.1-.8.5-.9 4.4-1 8.1-.6 11.2 1.3.3.2.4.7.3 1zm1.5-3.3c-.3.4-.8.5-1.2.3-3.1-1.9-7.8-2.5-11.5-1.3-.5.1-1-.1-1.1-.6-.1-.5.1-1 .6-1.1 4.2-1.3 9.4-.7 13 1.5.3.2.5.7.2 1.2zm.1-3.4c-3.7-2.2-9.9-2.4-13.4-1.3-.5.2-1.1-.1-1.3-.7-.2-.5.1-1.1.7-1.3 4.1-1.2 10.8-1 15 1.5.5.3.7.9.4 1.4-.3.5-.9.6-1.4.4z"/></svg>
            Spotify
          </a>
          <a href="https://www.youtube.com/@CXMixer" target="_blank" rel="noopener" class="btn btn--ghost">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor"><path d="M23.5 6.2a3 3 0 0 0-2.1-2.1C19.5 3.5 12 3.5 12 3.5s-7.5 0-9.4.6A3 3 0 0 0 .5 6.2 31.9 31.9 0 0 0 0 12a31.9 31.9 0 0 0 .5 5.8 3 3 0 0 0 2.1 2.1c1.9.6 9.4.6 9.4.6s7.5 0 9.4-.6a3 3 0 0 0 2.1-2.1c.4-1.9.5-5.8.5-5.8s0-3.9-.5-5.8zM9.5 15.6V8.4l6.3 3.6-6.3 3.6z"/></svg>
            YouTube
          </a>
        </div>
      </div>
    </div>
  </section>

  <!-- ═══ TESTIMONIALS ═══ -->
  <section class="section quotes">
    <div class="container">
      <div class="quotes__grid reveal">
        <blockquote class="quotes__card">
          <div class="quotes__mark" aria-hidden="true">&ldquo;</div>
          <p>CX Mixer feels like the part of the conference you actually remember.</p>
          <cite>Brand CX Leader</cite>
        </blockquote>
        <blockquote class="quotes__card quotes__card--accent">
          <div class="quotes__mark" aria-hidden="true">&ldquo;</div>
          <p>Real conversations, real relationships, zero forced networking.</p>
          <cite>E-commerce Operator</cite>
        </blockquote>
        <blockquote class="quotes__card">
          <div class="quotes__mark" aria-hidden="true">&ldquo;</div>
          <p>Finally, a community that feels smart, welcoming, and actually enjoyable.</p>
          <cite>DTC Brand Founder</cite>
        </blockquote>
      </div>
    </div>
  </section>

  <!-- ═══ SPONSORS TEASER ═══ -->
  <section class="section section--coral sponsor-tease">
    <div class="sponsor-tease__deco" aria-hidden="true">&#x2726;</div>
    <div class="container text-center">
      <div class="reveal" style="max-width: 600px; margin: 0 auto;">
        <h2 class="mb-md">Partner with the community brands <em class="serif">want</em> to be part of</h2>
        <p style="max-width: 50ch; margin: 0 auto var(--sp-lg); opacity: 0.85;">
          CX Mixer works with select sponsors who want to show up in the right rooms, support authentic connection, and build real visibility with e-commerce decision-makers.
        </p>
        <a href={`${base}sponsors/`} class="btn btn--white">Become a Sponsor</a>
      </div>
    </div>
  </section>

  <!-- ═══ FINAL CTA ═══ -->
  <section class="section cta-final">
    <div class="container text-center">
      <div class="reveal" style="max-width: 650px; margin: 0 auto;">
        <h2 class="mb-md text-balance">Come hang with the <em class="serif">good side</em> of e-commerce</h2>
        <p style="max-width: 45ch; margin: 0 auto var(--sp-lg); font-size: 1.1rem; color: var(--text-muted);">
          Come for the event. Stay for the people.
        </p>
        <div style="display: flex; gap: var(--sp-sm); justify-content: center; flex-wrap: wrap;">
          <a href={`${base}collective/`} class="btn btn--primary">Join the Collective</a>
          <a href={`${base}events/`} class="btn btn--secondary">Get Event Updates</a>
        </div>
      </div>
    </div>
  </section>

</Base>

<style>
  /* ═══════════════════════════════════
     HERO — full-screen, dramatic
     ═══════════════════════════════════ */
  .hero {
    position: relative;
    min-height: 100vh;
    min-height: 100dvh;
    display: flex;
    align-items: center;
    padding-top: 76px;
    overflow: hidden;
    background: var(--ink);
  }

  .hero__bg {
    position: absolute;
    inset: 0;
    overflow: hidden;
  }

  .hero__orb {
    position: absolute;
    border-radius: 50%;
    filter: blur(100px);
    will-change: transform;
  }

  .hero__orb--1 {
    width: 700px; height: 700px;
    background: radial-gradient(circle, rgba(255, 94, 53, 0.3), transparent 70%);
    top: -250px; right: -150px;
    animation: float 20s ease-in-out infinite;
  }

  .hero__orb--2 {
    width: 500px; height: 500px;
    background: radial-gradient(circle, rgba(0, 212, 170, 0.2), transparent 70%);
    bottom: -150px; left: -100px;
    animation: float 25s ease-in-out infinite reverse;
  }

  .hero__orb--3 {
    width: 350px; height: 350px;
    background: radial-gradient(circle, rgba(124, 58, 237, 0.15), transparent 70%);
    top: 45%; left: 55%;
    animation: float 18s ease-in-out infinite 3s;
  }

  .hero__grid {
    position: absolute;
    inset: 0;
    background-image:
      linear-gradient(rgba(255,255,255,0.025) 1px, transparent 1px),
      linear-gradient(90deg, rgba(255,255,255,0.025) 1px, transparent 1px);
    background-size: 80px 80px;
    mask-image: radial-gradient(ellipse at 30% 50%, black 30%, transparent 75%);
    -webkit-mask-image: radial-gradient(ellipse at 30% 50%, black 30%, transparent 75%);
  }

  /* Decorative floating symbols */
  .hero__deco {
    position: absolute;
    pointer-events: none;
    animation: spin-slow 30s linear infinite;
  }

  .hero__deco--star {
    font-size: 3rem;
    color: var(--coral);
    opacity: 0.25;
    top: 18%;
    right: 12%;
    animation: pulse-glow 4s ease-in-out infinite;
  }

  .hero__deco--cross {
    font-family: var(--font-display);
    font-size: 4rem;
    font-weight: 800;
    color: var(--teal);
    opacity: 0.12;
    bottom: 25%;
    right: 22%;
    animation: spin-slow 40s linear infinite reverse;
  }

  .hero__deco--ring {
    width: 180px;
    height: 180px;
    border: 3px solid var(--electric);
    border-radius: 50%;
    opacity: 0.08;
    top: 30%;
    right: 5%;
    animation: spin-slow 50s linear infinite;
  }

  .hero__content {
    position: relative;
    z-index: 1;
    padding: var(--sp-3xl) 0;
    max-width: 780px;
  }

  .hero__eyebrow { margin-bottom: var(--sp-md); }

  .hero__title {
    color: #fff;
    margin-bottom: var(--sp-md);
    line-height: 1.0;
  }

  .hero__em {
    display: block;
    background: linear-gradient(135deg, var(--coral) 0%, var(--lemon) 50%, var(--teal) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    padding-top: 0.1em;
  }

  .hero__em .serif {
    font-size: 1.1em;
  }

  .hero__sub {
    color: rgba(255,255,255,0.55);
    font-size: 1.1rem;
    line-height: 1.75;
    margin-bottom: var(--sp-lg);
    max-width: 52ch;
  }

  .hero__actions {
    display: flex;
    gap: var(--sp-sm);
    flex-wrap: wrap;
    margin-bottom: var(--sp-xl);
  }

  .hero__listen {
    display: flex;
    align-items: center;
    gap: var(--sp-sm);
  }

  .hero__listen-label {
    color: rgba(255,255,255,0.3);
    font-size: 0.8rem;
    font-weight: 500;
    letter-spacing: 0.05em;
    text-transform: uppercase;
    font-family: var(--font-display);
  }

  .hero__listen-icon {
    color: rgba(255,255,255,0.3);
    transition: all var(--fast) var(--ease-bounce);
    display: flex;
  }

  .hero__listen-icon:hover {
    color: var(--coral);
    transform: scale(1.2) translateY(-2px);
  }

  /* Scroll indicator */
  .hero__scroll {
    position: absolute;
    bottom: 2rem;
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
    color: rgba(255,255,255,0.25);
    font-family: var(--font-display);
    font-size: 0.65rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.2em;
  }

  .hero__scroll-line {
    width: 1.5px;
    height: 40px;
    background: linear-gradient(to bottom, var(--coral), transparent);
    animation: pulse-glow 2s ease-in-out infinite;
  }

  /* ═══ INTRO ═══ */
  .intro__layout {
    display: grid;
    grid-template-columns: 1.1fr 1fr;
    gap: var(--sp-2xl);
    align-items: start;
  }

  .intro__cards {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }

  .intro__card {
    display: flex;
    align-items: flex-start;
    gap: var(--sp-md);
    padding: var(--sp-md) var(--sp-lg);
    background: var(--surface);
    border-radius: var(--r-lg);
    border: 2px solid var(--border);
    transition: all var(--med) var(--ease);
    cursor: default;
  }

  .intro__card:hover {
    border-color: var(--coral);
    transform: translateX(6px);
    box-shadow: -6px 6px 0 var(--coral);
  }

  .intro__card-icon {
    font-size: 1.8rem;
    flex-shrink: 0;
    margin-top: 2px;
  }

  .intro__card h4 { margin-bottom: 0.1rem; font-size: 1.05rem; }
  .intro__card p { font-size: 0.88rem; color: var(--text-muted); line-height: 1.5; }

  @media (max-width: 768px) {
    .intro__layout { grid-template-columns: 1fr; gap: var(--sp-xl); }
  }

  /* ═══ EVENTS ═══ */
  .events__head { margin-bottom: var(--sp-xl); }

  .events__head .section-label::before { background: var(--teal); }

  .events__grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--sp-md);
  }

  .events__card {
    background: rgba(255,255,255,0.03);
    border: 2px solid rgba(255,255,255,0.08);
    border-radius: var(--r-xl);
    padding: var(--sp-lg);
    position: relative;
    overflow: hidden;
    transition: all var(--med) var(--ease);
  }

  .events__card:hover {
    border-color: var(--coral);
    transform: translateY(-6px) rotate(-0.5deg);
    box-shadow: 0 20px 50px rgba(255, 94, 53, 0.12);
  }

  .events__card--featured {
    border-color: var(--coral);
    background: rgba(255, 94, 53, 0.06);
  }

  .events__card-num {
    font-family: var(--font-display);
    font-weight: 800;
    font-size: 5rem;
    color: rgba(255,255,255,0.04);
    position: absolute;
    top: -0.5rem;
    right: 1rem;
    line-height: 1;
    pointer-events: none;
  }

  .events__card h3 {
    color: #fff;
    font-size: 1.3rem;
    margin: 0.6rem 0 0.4rem;
  }

  .events__card p {
    color: rgba(255,255,255,0.5);
    font-size: 0.9rem;
    margin-bottom: var(--sp-md);
  }

  .events__card-tag {
    font-family: var(--font-display);
    font-weight: 700;
    font-size: 0.75rem;
    color: var(--teal);
    text-transform: uppercase;
    letter-spacing: 0.08em;
  }

  @media (max-width: 768px) {
    .events__grid { grid-template-columns: 1fr; }
  }

  /* ═══ BRANDS ═══ */
  .brands { padding-bottom: var(--sp-xl); overflow: hidden; }

  .brands__pill {
    font-family: var(--font-display);
    font-weight: 700;
    font-size: 0.95rem;
    color: var(--ink);
    padding: 0.55rem 1.4rem;
    margin: 0 0.35rem;
    background: var(--surface);
    border-radius: var(--r-pill);
    border: 2px solid var(--border);
    white-space: nowrap;
    transition: all var(--fast) var(--ease);
    letter-spacing: -0.01em;
  }

  .brands__pill:hover {
    border-color: var(--coral);
    color: var(--coral);
    transform: scale(1.05);
  }

  .brands__pill--alt {
    background: var(--ink);
    color: rgba(255,255,255,0.7);
    border-color: rgba(255,255,255,0.1);
  }

  .brands__pill--alt:hover {
    border-color: var(--teal);
    color: var(--teal);
  }

  /* ═══ COMMUNITY ═══ */
  .community__layout {
    display: grid;
    grid-template-columns: 1fr 1.1fr;
    gap: var(--sp-2xl);
    align-items: center;
  }

  .community__visual {
    position: relative;
    height: 380px;
  }

  .community__blob {
    position: absolute;
    width: 260px;
    height: 260px;
    border-radius: 50%;
    background: linear-gradient(135deg, rgba(0, 212, 170, 0.15), rgba(124, 58, 237, 0.1));
    filter: blur(60px);
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
  }

  .community__float {
    position: absolute;
    background: var(--surface);
    border-radius: var(--r-lg);
    padding: 0.85rem 1.2rem;
    display: flex;
    align-items: center;
    gap: 0.75rem;
    font-family: var(--font-display);
    font-weight: 600;
    font-size: 0.85rem;
    box-shadow: 0 12px 40px rgba(10, 15, 28, 0.12);
    border: 2px solid var(--border);
    animation: float 6s ease-in-out infinite;
  }

  .community__float span:first-child { font-size: 1.4rem; }

  .community__float--1 { top: 15%; left: 5%; }
  .community__float--2 { top: 45%; right: 0%; animation-delay: 2s; }
  .community__float--3 { bottom: 10%; left: 12%; animation-delay: 4s; }

  .community__quote {
    font-family: var(--font-serif);
    font-style: italic;
    font-size: 1.1rem;
    color: var(--coral);
    border-left: 3px solid var(--coral);
    padding-left: var(--sp-md);
  }

  @media (max-width: 768px) {
    .community__layout { grid-template-columns: 1fr; }
    .community__visual { height: 260px; }
  }

  /* ═══ PODCAST ═══ */
  .podcast__head { margin-bottom: var(--sp-xl); }
  .podcast__head .section-label::before { background: var(--teal); }

  .podcast__list {
    display: flex;
    flex-direction: column;
    gap: 2px;
    border-radius: var(--r-lg);
    overflow: hidden;
  }

  .podcast__ep {
    display: grid;
    grid-template-columns: 80px 1fr 56px;
    gap: var(--sp-md);
    align-items: center;
    padding: var(--sp-md) var(--sp-lg);
    background: rgba(255,255,255,0.03);
    transition: all var(--fast) var(--ease);
    text-decoration: none;
    color: inherit;
  }

  .podcast__ep:hover {
    background: rgba(255,255,255,0.06);
  }

  .podcast__ep:hover .podcast__ep-play {
    transform: scale(1.15);
    box-shadow: 0 8px 24px rgba(255, 94, 53, 0.4);
  }

  .podcast__ep-num {
    font-family: var(--font-display);
    font-weight: 800;
    font-size: 2rem;
    color: rgba(255,255,255,0.08);
    text-align: center;
  }

  .podcast__ep-info h3 {
    color: #fff;
    font-size: 1.1rem;
    margin-bottom: 0.15rem;
  }

  .podcast__ep-info p {
    color: rgba(255,255,255,0.4);
    font-size: 0.85rem;
  }

  .podcast__ep-play {
    width: 48px;
    height: 48px;
    border-radius: 50%;
    background: var(--coral);
    color: #fff;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all var(--med) var(--ease-bounce);
    flex-shrink: 0;
  }

  .podcast__ep-play svg { margin-left: 3px; }

  .podcast__ctas {
    display: flex;
    gap: var(--sp-sm);
    justify-content: center;
    flex-wrap: wrap;
  }

  @media (max-width: 768px) {
    .podcast__ep { grid-template-columns: 1fr 48px; }
    .podcast__ep-num { display: none; }
  }

  /* ═══ TESTIMONIALS ═══ */
  .quotes__grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--sp-md);
  }

  .quotes__card {
    background: var(--surface);
    border-radius: var(--r-xl);
    padding: var(--sp-xl) var(--sp-lg);
    border: 2px solid var(--border);
    position: relative;
    transition: all var(--med) var(--ease);
  }

  .quotes__card:hover {
    transform: translateY(-6px) rotate(0.5deg);
    box-shadow: 0 20px 50px rgba(10, 15, 28, 0.1);
    border-color: var(--teal);
  }

  .quotes__card--accent {
    background: var(--ink);
    border-color: var(--ink);
    color: rgba(255,255,255,0.85);
  }

  .quotes__card--accent .quotes__mark { color: var(--coral); }
  .quotes__card--accent p { color: #fff; }
  .quotes__card--accent cite { color: rgba(255,255,255,0.4); }

  .quotes__card--accent:hover {
    border-color: var(--coral);
    transform: translateY(-6px) rotate(-0.5deg);
  }

  .quotes__mark {
    font-family: var(--font-serif);
    font-size: 5rem;
    color: var(--teal);
    opacity: 0.3;
    line-height: 0.6;
    margin-bottom: 0.5rem;
    display: block;
  }

  .quotes__card p {
    font-size: 1.05rem;
    font-weight: 500;
    color: var(--ink);
    margin-bottom: var(--sp-sm);
    line-height: 1.5;
  }

  .quotes__card cite {
    font-style: normal;
    font-size: 0.8rem;
    color: var(--mist);
    font-weight: 600;
    font-family: var(--font-display);
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  @media (max-width: 768px) {
    .quotes__grid { grid-template-columns: 1fr; }
  }

  /* ═══ SPONSORS TEASER ═══ */
  .sponsor-tease {
    position: relative;
    overflow: hidden;
  }

  .sponsor-tease__deco {
    position: absolute;
    font-size: 20rem;
    opacity: 0.08;
    top: -3rem;
    right: -2rem;
    pointer-events: none;
    animation: spin-slow 60s linear infinite;
  }

  /* ═══ FINAL CTA ═══ */
  .cta-final {
    padding: var(--sp-3xl) 0;
    position: relative;
    overflow: hidden;
  }

  .cta-final::before {
    content: '';
    position: absolute;
    width: 500px;
    height: 500px;
    border-radius: 50%;
    background: radial-gradient(circle, rgba(255, 94, 53, 0.06), transparent 70%);
    top: -200px;
    right: -100px;
    pointer-events: none;
  }
</style>
